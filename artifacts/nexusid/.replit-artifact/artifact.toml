kind = "web"
previewPath = "/"
title = "NexusID"
version = "1.0.0"
id = "artifacts/nexusid"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22653

[services.development]
run = "pnpm --filter @workspace/nexusid run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nexusid", "run", "build" ]
publicDir = "artifacts/nexusid/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22653"
BASE_PATH = "/"
